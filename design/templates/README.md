# テンプレート

区分(`slide`・`sheet`・`document`)ごとの見た目一式。`<区分>/<名前>/` に置く。

| ファイル | 中身 |
| --- | --- |
| `template.json` | 表示名(英語。識別子の頭を大文字にした名前)・説明・tokens の差分・部品の変種・レイアウト(質問票と文書) |
| `template.css` | 任意。そのテンプレートだけの CSS(下の「専用の CSS」) |
| `sample.json` | 任意。スライドの中身の見本 |
| `sample.en.json` | 任意。`sample.json` の英語。設定の言語(`locale`)が英語のとき一覧・編集画面・新規作成が使い、無ければ `sample.json` に落ちる |
| `assets/*.svg` | 任意。スライドのテンプレートに同梱する絵(小文字・数字・`-` の名前)。見本の `image` は `src` に `assets/<ファイル>` と書く。下の「同梱の絵」 |

値(色・書体・大きさ・余白・角・影)と部品の変種・レイアウトで足りるなら `template.json` だけで作る。見出しの作り・帯・背景の飾り・カードの並びなど、変数では表せない見た目だけを `template.css` に書く。

## 同梱の絵(assets/)

スライドのテンプレートは、見本に置く絵を `assets/*.svg` に持てる。いまは Default 以外の7件が `hero`(表紙の右)・`idea`(2列の片側)・`path`(締め)の3点を持つ。どれも 480x360 の自作の抽象的な図形で、写真やダウンロードした素材は入れない。

- **色:** `<img>` で読むので CSS の変数は届かない。SVG の中に直書きしてよいが、そのテンプレートの `tokens`(色と影)と同じ値だけを使う
- **同じ `src` が3か所で通る:** 見本の `image` は `src` に `assets/<ファイル>` と書く
  - 見本: `pnpm design:build` が `design/samples/slide.<名前>/assets/` へ写す。見本の HTML(`samples/slide.<名前>.html`)も画面の見本(一覧のカード・編集画面・新規作成)も、そこを基点に読む
  - 作った資料: 「この見た目で作成」(`createDeckFromOutline`)が、見た目のテンプレートと中身の構成の `assets/` を資料の `assets/` へ写す(同じ名前なら構成の方を残す)。編集画面は `/api/decks/:id/assets/` から読む
  - 書き出し: PDF はそのまま、PPTX は SVG を貼れないので撮った PNG に替えて入れる(`app/server/exporter.ts`)
- **複製:** テンプレートを複製すると `assets/` も写る。画面の見本に出るのは次の `pnpm design:build` の後
- 絵は中身(`image` ブロック)で、飾り(`.ds-decoration__shape--*`)は CSS。この線引きは変えない

## 中身の見本(sample.json)

`default` 以外の7件が中身の見本を持つ。題材はどれも同じ(このアプリの紹介)で、横に並べたとき構図と色の差だけが見えるようにしてある。

- 6枚を基本にする。表紙(左に題・右に絵)/ 章扉(章番号と題だけ)/ KPI と一言 / 2列で片側に絵 / 流れか比較 / 締め(一言と絵)
- 1枚に部品を2〜3つ置き、下半分を空けない
- 絵は `assets/` の同梱の絵を `image` ブロックで置く。飾り(`.ds-decoration__shape--*`)は CSS が描く
- 一覧のカードに出るのは表紙だけ(`coverSlide`)

### 英語の見本(sample.en.json)

中身の見本を持つ12件(見た目の7件と中身の構成5件)は、隣に英語の `sample.en.json` を持つ。

- **形は日本語と同じ:** ページとブロックの id・種類・座標は `sample.json` のまま、文だけを訳す(`templates.test.ts` が形の一致と日本語の取り残しを確かめる)。`sample.json` の配置を変えたら `sample.en.json` も合わせる
- **字数は英語で測る:** 英語は同じ意味でも幅を取る。KPI の数字は単位をラベルへ移して短くする(`4形式` → 数字 `4`・ラベル `Export formats`)など、枠に収まる言い方にする
- **未記入の印はそのまま:** `[[要確認]]` は英語の資料でも同じ印なので訳さない
- **選び方:** サーバーが設定の `locale` で読む(`readSlideSample`)。無ければ日本語に落ちる。複製すると `sample.en.json` も写る
- **見本の HTML:** `pnpm design:build` が `design/samples/en/` に同じ名前で英語の見本を作る(スライド・文書・質問票)。同梱の絵は `samples/slide.<名前>/assets/` の1組を読む。文書と質問票の英語は `app/server/document-sample.en.ts`・`sheet-sample.en.ts` の表(日本語の文 → 英語)で置き換える。見た目の検査(`template-css.export.test.ts`)は英語の見本も測る
- **共通の見本:** 見本を持たない `default` は、開発用のブロック一覧(`app/src/dev/block-catalog.json`)の英語 `block-catalog.en.json` に落ちる
- **部品一覧:** 編集画面で見本の後ろに足す2枚(`app/src/pages/design/parts.ts`)も、画面の言語で文を選ぶ

## 専用の CSS(template.css)

build は `template.css` を、そのテンプレートの変数の後ろへ入れ子で閉じ込めて `dist/<区分>/<名前>.css` に入れる。

| 区分 | 閉じ込める根 | 書いた `.ds-heading { … }` が効く範囲 |
| --- | --- | --- |
| スライド | `.ds-slide[data-template="<名前>"]` | そのテンプレートのスライドの中だけ。1画面に別のテンプレートのスライドが並んでも混ざらない |
| 質問票・文書 | `:root` | そのファイル全体(1ファイルに1テンプレート) |

根が1段つくので、同じセレクタなら部品の既定(`slide.css`・`document.css`・`interaction.css`)より強く、読み込み順を気にしなくてよい。スライドの画面は `dist/slide-templates.css`(スライドの専用の CSS だけをまとめたもの)を読む。

### 書き方

- DOM は区分ごとに共通のまま使う。class や要素を足さず、既存の `.ds-*` を CSS だけで変える。新しい並びが要るときは描画側(スライドは `app/src/renderer/`、質問票は `app/server/sheet-sample.ts` と質問票スキルの client.js、文書は見本と文書スキル)の作業にする
- 色と大きさは変数(`var(--color-*)`・`var(--space-*)`・`var(--fs-*)`・`var(--radius-*)` など)から引く。値を変えたいときは `template.json` の tokens に書く。色の直書き(`#xxxxxx`)・`@import`・外の `url(http…)`・`</` は build が止める
- 文書の目次は、章の `ol` の中に節の `ol` が入れ子で入る。共通の CSS は節を隠し(`.ds-toc ol ol`)、節を出すテンプレートは `.ds-toc ol ol` に display を書いて出す。章の並び(横並びなど)を変えるときは `.ds-toc > ol` と書く。専用の CSS は共通より強いので、`.ds-toc ol` に display を書くと節にも効いて節が目次に出る。これは build が止める
- 入れ子の中なので、根そのものは `&`、根の class との組み合わせは `&.ds-slide--cover` のように書く。要素セレクタ(`body`・`h1`)もそのまま書ける(`:root body` になる)
- スライドの飾りは `.ds-decoration__shape--1`〜`--6`(既定は `display: none`)に描く。どのページにもある空の箱で、塗りと線は PPTX にも箱として出る。疑似要素(`::before`・`::after`)は PPTX に出ないので、見出しや本文の地にしない。見出しの縦棒や左の帯は `border-left` などの辺の枠で描く。手描きの枠は `.ds-sketch`(SVG。既定は `display: none`)で、色は `.ds-sketch__line` の `stroke`。PPTX には撮った画像で入るので、使うテンプレートの `description` に書く
- PPTX に出るもの: 塗り、枠線(辺ごとに違う幅・色も、その辺にだけ出る。角丸の箱で辺ごとに違う枠は、角の丸みを持たない辺の帯で近似する)、表のセルごとの罫(見出しの下だけの線・横罫だけ・縞)、字間(`letter-spacing`)、下線(`text-decoration-line: underline`。太さは落ち、字の色の一重の線になる)、手順の段のつなぎ(`.ds-process__link`。矢印や線を持つ要素で、疑似要素にしない)。落ちるもの: 疑似要素、影、半透明(不透明の色になる)、枠線の点線・破線(実線になる)、表の角丸
- スライドのブロックは `deck.json` の座標で置く。ブロックの位置と大きさは CSS で動かさない(編集画面の選択枠とずれる)。見出しを大きくしたり枠を足したりすると、ブロック枠からはみ出しやすい
- 表紙と締めは `.ds-slide--cover`・`.ds-slide--closing`、章扉は `.ds-slide--section`、本文は `.ds-slide--content`。全面を塗るときは、中の箱の部品(`.ds-card`・`.ds-kpi`・`.ds-table` など)の字の色を本文色に戻す

### 確かめる

1. `pnpm design:build`(`dist/` と見本を作り直す)
2. 画面のテンプレートの一覧と編集で見本を見る。質問票と文書の見本は `/api/design/files/samples/<見本>.html?template=<名前>` でも開ける
3. `pnpm test:export` の `template-css.export.test.ts` が、専用の CSS を当てた見本を実ブラウザで描き、文字の明暗差(本文 4.5・大きい字 3、スライドは 3)とはみ出しを全テンプレートで測る

## 用意したテンプレート

| 区分 | 名前 | 見た目 |
| --- | --- | --- |
| スライド | `default` | 白と薄い青。表紙と締めにウェーブ。中身の見本は持たず、共通の見本に落ちる |
| スライド | `linen` | 静かな余白。生成りの地と細い罫線、細い見出し、表紙の右に淡い面 |
| スライド | `chalk` | 研修。薄灰の地に細い外枠と黒い点線、カードはオレンジ・ピンク・紫。表紙と章扉は中央寄せ |
| スライド | `crayon` | ノート。丸ゴシックの見出し、黄・青・ピンクの面に黒い枠、手描きの枠と絵の余白 |
| スライド | `cobalt` | 公共の案内。白地に濃い青、見出しの左の縦棒、影のない灰の細い枠のカード、罫と縞の表、番号の丸でつなぐ手順。字は明暗差 4.5 以上・18px 以上 |
| スライド | `podium` | 登壇用の白地(黒地の案と見比べて選んだ)。本文 32・見出し 44(`h1` 48)・表紙 72、差し色は深紅の1色で章扉は深紅の全面、表は罫だけ。字は明暗差 4.5 以上・24px 以上 |
| 質問票 | `default` | 白いカードと青。1問ずつ |
| 質問票 | `paper` | 明朝の書類。罫線の記入欄に全問を並べる |
| 質問票 | `cobalt` | 申請フォーム。全問を並べる(4つのレイアウトすべてに対応)。濃い青の選択肢、高さ 48 の記入欄、「※必須」、黄と黒のフォーカス、番号の丸の質問一覧 |
| 文書 | `default` | 白い紙1枚と青。目次は上、脇は右 |
| 文書 | `report` | 報告書。濃紺の署名帯、左の目次、章の番号と縦線 |
| 文書 | `cobalt` | 公共機関の資料でよく見る組み。本文16/1.7、見出しに肩と左の縦棒、罫つきの表、帯つきの注記、左の目次 |
| 文書 | `documentation` | 長い手順書・設計書の読み物。左に章と節の目次・中央に本文・右に用語の3列(`head: main` で題名は本文の列の上)。目次と用語はスクロールしても画面に残り、読んでいる章・節が緑になる。コードと注記は角丸の細い枠の箱、手順は番号の丸と縦の線、表は罫だけ。1080px 以下は用語を本文の下へ、760px 以下と印刷は縦に並べる |

## Cobalt

デジタル庁デザインシステム https://design.digital.go.jp/dads/ をもとに作成。値(色の段・字の段・罫の太さ・角丸・余白)は部品集の HTML 版 [design-system-example-components-html](https://github.com/digital-go-jp/design-system-example-components-html) の CSS から写し、スライド・文書・質問票に置くために手を加えている(字の段の選び方、スライドの12列の格子とページの型、明暗差の検査に合わせた色)。デザインシステムの提供元が作ったものではない。出典と MIT の全文は [NOTICE](../../NOTICE) にある。

画面・`label`・`description`・識別子・CSS には名前を出さない。出典はこの節と NOTICE にだけ書く。

- スライド(`slide/cobalt/`): 字は部品集の段(64・57・45・36・28・24・20・18)からだけ選び、`checks` で明暗差 4.5・18px 以上を測る。同梱の絵は部品集の色の段の値だけで塗る
- 文書(`document/cobalt/`): 本文 16 / 1.7。`checks` で明暗差 4.5・14px 以上を測る。注記の地は部品集に合わせて白に寄せた
- 質問票(`sheet/cobalt/`): 4つのレイアウトすべてに対応。`checks` で明暗差 4.5・14px 以上を測る。段に無い値(17 の字など)は近い値で代えている

部品集の著作権表示と許諾の文(MIT)は [NOTICE](../../NOTICE) に全文がある。
