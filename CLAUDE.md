@AGENTS.md

## Claude cloud sessions

In a Claude cloud session (`CLAUDE_CODE_REMOTE=true`), the user can't see the screen. Finish every change by following `.claude/skills/cloud-pr/SKILL.md`: take before/after screenshots and open a pull request. The user asked for this in advance, so open the PR without asking again.
